#include <stdio.h>

/**
 * main - Execute base 10 no
 *
 * Return: return 0 as success
 **/
int main(void)
{
	char n;

	for (n = '0'; n <= '9'; n++)
	{
		putchar(n);
	}

	putchar('\n');

	return (0);
}
